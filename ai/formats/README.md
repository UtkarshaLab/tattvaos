# ai/formats — Model File Formats

> Parsers for AI model file formats.

Supports: GGUF, SafeTensors, ONNX.
Validates headers, maps weights to memory, handles quantized types.

Part of the `ai/` category in Tattva OS.
