# ai/utrain — Training Infrastructure

> Fine-tuning and training loop infrastructure.

Implements: gradient computation, optimizer steps (AdamW, SGD),
LoRA/QLoRA adapters, checkpoint save/load, mixed precision training.

Part of the `ai/` category in Tattva OS.
