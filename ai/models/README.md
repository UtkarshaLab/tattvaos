# ai/models — Model Architectures

> Model architecture definitions and forward pass implementations.

Supports: LLaMA, Mistral, Mixtral, Phi, Qwen, custom architectures.
Each model implements its own attention variant and feed-forward block.

Part of the `ai/` category in Tattva OS.
