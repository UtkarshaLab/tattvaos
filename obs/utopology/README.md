# obs/utopology — Hardware Topology Visualizer

> Visualizes hardware topology for Tattva OS nodes.

Shows: NUMA nodes, CPU sockets, cores, cache hierarchy, memory bandwidth
paths — as ASCII art or structured data for dashboards.

Part of the `obs/` category in Tattva OS.
