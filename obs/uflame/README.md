# obs/uflame — Flame Graph Renderer

> Renders flame graphs from profiler output.

Takes raw sample data from uprof, produces SVG flame graphs.
Supports differential flame graphs (before/after comparison).

Part of the `obs/` category in Tattva OS.
