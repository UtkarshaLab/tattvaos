# obs/umap_proc — Process Memory Map Visualizer

> Shows the memory layout of a running Tattva process.

Displays: code, data, stack, heap, mmap regions with sizes and permissions.
Useful for understanding memory consumption and layout.

Part of the `obs/` category in Tattva OS.
