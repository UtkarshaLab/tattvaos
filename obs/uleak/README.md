# obs/uleak — Memory Leak Detector

> Memory leak detection for Tattva OS.

Tracks all allocations and frees, reports objects that were never freed.
Reports: allocation site, size, age, call stack at allocation time.

Part of the `obs/` category in Tattva OS.
