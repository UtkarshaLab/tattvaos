# obs/uprof — CPU Profiler

> Sampling-based CPU profiler for Tattva OS.

Implements: PMU-based sampling, call stack capture, hot path identification,
output as raw samples for uflame to render.

Part of the `obs/` category in Tattva OS.
