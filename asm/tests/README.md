# asm/tests — utasm Test Suite

> Tests for the entire utasm toolchain.

Covers: assembler correctness, disassembler round-trips,
linker output, linter detection accuracy, safety checker rules.

Part of the `asm/` category in Tattva OS.
