# kernel/ipc — Inter-Process Communication

> IPC primitives for Tattva OS.

Implements: message queues, shared memory regions, lock-free ring buffers,
event notification, synchronization primitives (mutex, semaphore, condvar).

Part of the `kernel/` category in Tattva OS.
