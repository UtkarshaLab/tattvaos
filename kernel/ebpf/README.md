# kernel/ebpf — eBPF Runtime

> Safe dynamic instrumentation for Tattva OS.

Implements: eBPF bytecode verifier, JIT compiler (x86-64),
map types (hash, array, ring buffer), tracepoint attachment.

Part of the `kernel/` category in Tattva OS.
