# kernel/tests — Kernel Tests

> Unit and integration tests for the Tattva OS kernel.

Covers: memory allocator correctness, scheduler fairness,
syscall behavior, IPC semantics, driver I/O paths.
All tests run in QEMU — no real hardware required.

Part of the `kernel/` category in Tattva OS.
