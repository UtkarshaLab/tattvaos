# kernel/drivers — Device Drivers

> Kernel device drivers for Tattva OS.

Includes: NVMe (direct queue access), UART, Intel/AMD NIC drivers,
PCIe enumeration, IOMMU setup, interrupt routing.

Part of the `kernel/` category in Tattva OS.
