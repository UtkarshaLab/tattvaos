# kernel/syscall — System Call Interface

> Minimal syscall table for Tattva OS.

Implements: Tattva-native syscalls for memory, I/O, scheduling, IPC.
No POSIX compatibility layer. Clean, minimal, purposeful.

Part of the `kernel/` category in Tattva OS.
