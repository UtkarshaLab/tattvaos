# kernel/mm — Memory Management

> Physical and virtual memory management for Tattva OS.

Implements: buddy allocator, slab allocator, virtual address space,
huge page support (2MB, 1GB), page table management, TLB shootdown.

Part of the `kernel/` category in Tattva OS.
