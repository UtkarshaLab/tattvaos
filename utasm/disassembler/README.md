# asm/disassembler — utasm Disassembler

> Takes a binary, emits human-readable assembly.

Supports: x86-64, AArch64, RISC-V.
Outputs: Intel syntax, AT&T syntax, annotated listing.

Part of the `asm/` category in Tattva OS.
