# asm/linker — utasm Linker

> Resolves symbols and produces final binary output.

Handles: symbol resolution, relocation, section merging,
ELF output, flat binary output, linker script support.

Part of the `asm/` category in Tattva OS.
