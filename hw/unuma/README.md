# hw/unuma — NUMA Topology

> NUMA topology discovery and memory placement policy.

Detects NUMA nodes, maps CPU-to-memory affinity,
enforces local-first allocation policy for latency-sensitive workloads.

Part of the `hw/` category in Tattva OS.
