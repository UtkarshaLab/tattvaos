# hw/uhbm — High Bandwidth Memory

> HBM2/HBM3 topology discovery and access for Tattva OS.

Detects HBM-attached stacks, maps their address ranges,
optimizes memory placement for AI kernels requiring high bandwidth.

Part of the `hw/` category in Tattva OS.
