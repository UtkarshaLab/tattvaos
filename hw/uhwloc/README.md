# hw/uhwloc — Hardware Locality

> Thread-to-core and memory-to-node binding.

Implements: core pinning, NUMA memory binding, cache-aware placement,
hardware topology export for the scheduler and memory manager.

Part of the `hw/` category in Tattva OS.
