# hw/ugpu — GPU Hardware Layer

> Direct GPU access for Tattva OS AI workloads.

Handles: PCIe BAR enumeration, command queue setup,
memory-mapped I/O, DMA buffer management, compute dispatch.

Part of the `hw/` category in Tattva OS.
