# hw/ucpu — CPU Hardware Layer

> CPU topology and feature access for Tattva OS.

Handles: core enumeration, CPUID feature detection, APIC initialization,
frequency detection, thread/core/socket topology mapping.

Part of the `hw/` category in Tattva OS.
