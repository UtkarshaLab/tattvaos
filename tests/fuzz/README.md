# tests/fuzz — Fuzz Tests

> Structure-aware fuzz testing for Tattva OS.

Fuzzes: BXP parser, HTTP/3 frame parser, config file parser,
ELF loader, DNS response parser. Uses coverage-guided mutation.

Part of the `tests/` category in Tattva OS.
