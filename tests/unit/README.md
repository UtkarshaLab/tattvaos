# tests/unit — Unit Tests

> Isolated unit tests for individual functions and modules.

No hardware required. Each test runs entirely in memory.
Coverage target: every public function in every library.

Part of the `tests/` category in Tattva OS.
