# tests/qemu — QEMU Boot Tests

> Automated boot verification in QEMU for Tattva OS.

Boots the full kernel image, captures UART output, compares against
expected output for each boot stage. Runs in CI on every commit.

Part of the `tests/` category in Tattva OS.
