# tests/bench — Benchmark Regression Tests

> Benchmark regression testing for Tattva OS.

Compares current performance against a stored baseline.
Fails if any benchmark regresses by more than 5%.

Part of the `tests/` category in Tattva OS.
