# tests/integration — Integration Tests

> Subsystem interaction tests for Tattva OS.

Tests: memory + scheduler interaction, network + storage pipelines,
inference + serving end-to-end within a single process.

Part of the `tests/` category in Tattva OS.
