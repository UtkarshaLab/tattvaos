# storage/ubxp — BXP Binary Format

> BXP binary serialization format for storage and network.

Defines: frame structure, type system, schema evolution rules.
Used by: udb storage format, urpc wire format, uobject metadata.

Part of the `storage/` category in Tattva OS.
