# storage/ummapf — Memory-Mapped Files

> Memory-mapped file abstraction for Tattva OS.

Implements: mmap/munmap over uFS, demand paging for large files,
copy-on-write semantics, prefetch hints for sequential access.

Part of the `storage/` category in Tattva OS.
