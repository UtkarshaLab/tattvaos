# storage/unvme — NVMe Driver

> Direct NVMe hardware access for Tattva OS.

Bypasses the block layer entirely. Submits I/O directly to NVMe
submission queues. Supports: NVMe 1.4+, multiple namespaces, ZNS.

Part of the `storage/` category in Tattva OS.
