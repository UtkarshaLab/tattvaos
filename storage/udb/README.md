# storage/udb — Sagar Database

> Sagar — embedded database for Tattva OS.

Implements: B-tree index, LSM-tree storage engine, SQL-subset query language,
write-ahead log via uwal, MVCC for concurrent reads.

Part of the `storage/` category in Tattva OS.
