# lib/str — String Library

> String manipulation for Tattva OS — no libc required.

Implements: copy, compare, search, format (printf-style), split, trim.
All functions are bounds-safe and null-terminator aware.

Part of the `lib/` category in Tattva OS.
