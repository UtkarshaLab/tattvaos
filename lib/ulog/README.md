# lib/ulog — Logging Library

> Structured logging for Tattva OS.

Implements: log levels (debug/info/warn/error), ring buffer output,
structured key-value fields, UART and memory-mapped output backends.

Part of the `lib/` category in Tattva OS.
