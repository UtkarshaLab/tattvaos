# lib/mem — Memory Allocator

> Core memory allocation library for Tattva OS.

Implements: slab allocator, buddy allocator, arena/region allocator,
stack allocator. No heap fragmentation by design.

Part of the `lib/` category in Tattva OS.
