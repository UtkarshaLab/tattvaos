# lib/ulib — General Purpose Library

> General-purpose data structures and algorithms.

Implements: hash map, linked list, dynamic array, queue, priority queue,
binary search, sorting, bit manipulation utilities.

Part of the `lib/` category in Tattva OS.
