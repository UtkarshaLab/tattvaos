# lib/ucmp — Compression Library

> Data compression for Tattva OS.

Implements: LZ4 (fast, for storage hot path), Zstd (balanced),
block-level compression with configurable level.

Part of the `lib/` category in Tattva OS.
