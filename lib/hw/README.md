# lib/hw — Hardware Utility Library

> Shared hardware access utilities used across drivers.

Implements: MMIO read/write helpers, port I/O wrappers,
memory barriers, cache flush operations, PCI config space access.

Part of the `lib/` category in Tattva OS.
