# lib/cal — Calendar Library

> Calendar and date utilities for Tattva OS.

Implements: Gregorian calendar, Unix timestamp conversion,
date arithmetic, ISO 8601 formatting.

Part of the `lib/` category in Tattva OS.
