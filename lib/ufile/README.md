# lib/ufile — File Abstraction

> File operations built on top of uFS.

Provides: open, read, write, seek, close, stat — with a clean
interface over the raw uFS block layer.

Part of the `lib/` category in Tattva OS.
