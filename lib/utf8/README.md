# lib/utf8 — UTF-8 Library

> UTF-8 encoding and decoding for Tattva OS.

Implements: encode codepoint → UTF-8 bytes, decode UTF-8 → codepoints,
string length in codepoints, validation, BOM detection.

Part of the `lib/` category in Tattva OS.
