# lib/regex — Regex Engine

> Minimal, backtrack-free regex engine for Tattva OS.

Implements: NFA-based matching (no exponential backtracking),
character classes, quantifiers, anchors, capture groups.

Part of the `lib/` category in Tattva OS.
