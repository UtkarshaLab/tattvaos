# utils/archive — Archive Utilities

> Archive and compression utilities for Tattva OS.

Implements: pack/unpack archives (tar-compatible), compress/decompress
(using lib/ucmp), integrity verification, streaming extraction.

Part of the `utils/` category in Tattva OS.
