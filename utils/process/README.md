# utils/process — Process Utilities

> Process inspection and management utilities for Tattva OS.

Implements: list processes (ps), inspect (inspect), signal (kill),
wait, priority adjustment, resource usage display.

Part of the `utils/` category in Tattva OS.
