# utils/text — Text Processing Utilities

> Text processing utilities for Tattva OS.

Implements: grep (pattern search), sed (stream editor), awk (field processing),
sort, uniq, wc, head, tail, cut, join.

Part of the `utils/` category in Tattva OS.
