# utils/dir — Directory Utilities

> Directory management utilities for Tattva OS.

Implements: list (ls), create (mkdir), remove (rmdir), tree view,
recursive operations, directory traversal.

Part of the `utils/` category in Tattva OS.
