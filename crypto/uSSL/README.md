# crypto/uSSL — TLS 1.3

> Full TLS 1.3 implementation for Tattva OS.

Implements: handshake state machine, record layer, HKDF key derivation,
session resumption, 0-RTT, certificate verification.

Part of the `crypto/` category in Tattva OS.
