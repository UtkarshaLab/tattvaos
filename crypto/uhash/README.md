# crypto/uhash — Hashing Library

> Cryptographic and non-cryptographic hash functions.

Implements: SHA-256, SHA-3/Keccak, BLAKE3, xxHash, CRC32.
Uses SHA-NI hardware extensions when available for AES/SHA acceleration.

Part of the `crypto/` category in Tattva OS.
