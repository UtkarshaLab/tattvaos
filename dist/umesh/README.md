# dist/umesh — Service Mesh

> Inter-node communication fabric for Tattva OS clusters.

Handles: service discovery, request routing between nodes,
load balancing, circuit breaking, health-based routing.

Part of the `dist/` category in Tattva OS.
