# dist/uraft — Raft Consensus

> Raft consensus implementation for distributed Tattva OS clusters.

Implements: leader election, log replication, log compaction (snapshots),
membership changes, linearizable reads.

Part of the `dist/` category in Tattva OS.
