# platform/aarch64 — AArch64 Platform

> AArch64 specific implementation for Tattva OS.

Covers: GIC interrupt controller, EL1 init, system registers,
UEFI boot on server ARM (Ampere, Graviton, Apple Silicon).

Build after x86-64 path is solid.

Part of the `platform/` category in Tattva OS.
