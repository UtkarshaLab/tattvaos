# bench/inference — Inference Benchmarks

> End-to-end inference performance benchmarks.

Measures: time-to-first-token (TTFT), inter-token latency (ITL),
throughput (tokens/sec), memory usage per context, batch efficiency.

Part of the `bench/` category in Tattva OS.
