# bench/network — Network Stack Benchmarks

> Performance benchmarks for the Tattva OS network stack.

Measures: HTTP/3 requests/sec, TCP throughput, connection setup latency,
zero-copy performance, packet processing rate at various MTUs.

Part of the `bench/` category in Tattva OS.
