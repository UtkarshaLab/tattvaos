# bench/os — OS Primitive Benchmarks

> Performance benchmarks for kernel primitives.

Measures: context switch overhead, IPC message latency,
memory allocation throughput, syscall round-trip cost,
interrupt latency, scheduler jitter.

Part of the `bench/` category in Tattva OS.
