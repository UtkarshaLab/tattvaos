# bench/database — Database Benchmarks

> Performance benchmarks for Sagar (udb) database.

Measures: sequential read/write throughput, random I/O IOPS,
query latency distribution, WAL write amplification.

Part of the `bench/` category in Tattva OS.
