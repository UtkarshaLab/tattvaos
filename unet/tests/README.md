# net/tests — Network Tests

> Tests for the Tattva OS network stack.

Covers: TCP state machine, HTTP/3 request/response, DNS resolution,
packet parsing correctness, connection lifecycle edge cases.

Part of the `net/` category in Tattva OS.
