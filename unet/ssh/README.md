# net/uSSH — SSH Server

> SSH server for remote access to Tattva OS.

Implements: SSH-2 protocol, key-based authentication (Ed25519),
interactive shell session (ish), SFTP subsystem, port forwarding.

Part of the `net/` category in Tattva OS.
