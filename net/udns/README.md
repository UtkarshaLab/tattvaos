# net/udns — DNS Resolver

> DNS resolver and cache for Tattva OS.

Implements: recursive resolver, stub resolver, DNS-over-HTTPS (DoH),
DNSSEC validation, negative caching, TTL-based eviction.

Part of the `net/` category in Tattva OS.
