# net/urdma — RDMA

> Remote Direct Memory Access for Tattva OS clusters.

Implements: RDMA verb interface, InfiniBand / RoCE support,
queue pair setup, work request posting, completion polling.
Used for high-bandwidth tensor transfer in distributed inference.

Part of the `net/` category in Tattva OS.
