# net/unet — Core Network Stack

> Ethernet, IP, TCP, UDP implementation for Tattva OS.

Implements: NIC driver interface, Ethernet framing, ARP, IPv4/IPv6,
TCP state machine, UDP sockets, zero-copy packet path.

Part of the `net/` category in Tattva OS.
