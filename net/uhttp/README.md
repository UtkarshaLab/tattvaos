# net/uhttp — HTTP/3 Server

> HTTP/3 server with QUIC transport for Tattva OS.

Implements: QUIC connection management, HTTP/3 framing, HPACK compression,
server push, zero-copy response body handling, connection migration.

Part of the `net/` category in Tattva OS.
