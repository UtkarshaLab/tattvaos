# serve/userve — Core Serving Engine

> Request lifecycle and response streaming for Tattva OS.

Handles: request ingestion from uhttp, routing to inference engine,
streaming token output back to client, connection lifecycle.

Part of the `serve/` category in Tattva OS.
