# serve/urerank — Reranking Endpoint

> Cross-encoder reranking for RAG pipelines.

Takes a query and list of candidate passages, scores each with a
cross-encoder model, returns ranked results by relevance score.

Part of the `serve/` category in Tattva OS.
