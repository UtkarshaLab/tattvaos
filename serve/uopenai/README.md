# serve/uopenai — OpenAI-Compatible API

> OpenAI API compatibility layer for Tattva OS.

Implements: `/v1/chat/completions`, `/v1/completions`, `/v1/models`,
`/v1/embeddings` — request/response format compatible with OpenAI clients.

Part of the `serve/` category in Tattva OS.
