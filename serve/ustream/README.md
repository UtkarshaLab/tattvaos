# serve/ustream — Streaming Response

> Server-Sent Events and streaming token output for Tattva OS.

Implements: SSE framing (text/event-stream), chunked transfer encoding,
backpressure handling, client disconnect detection.

Part of the `serve/` category in Tattva OS.
