# serve/uaudit — Audit Log

> Request audit logging for Tattva OS.

Records: every request with timestamp, caller identity, model, token count.
Output: append-only log to storage, exportable to external systems.

Part of the `serve/` category in Tattva OS.
