# serve/ugate — API Gateway

> API gateway and request routing for Tattva OS.

Implements: path-based routing, rate limiting, request validation,
header manipulation, upstream selection, circuit breaking.

Part of the `serve/` category in Tattva OS.
