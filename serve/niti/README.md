# serve/niti — Firewall

> Inbound request firewall for Tattva OS serving layer.

Implements: IP allowlist/denylist, rate limiting by IP, geo-blocking,
request pattern filtering, DDoS mitigation primitives.

Part of the `serve/` category in Tattva OS.
