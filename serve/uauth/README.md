# serve/uauth — Authentication

> API authentication and authorization for Tattva OS serving layer.

Implements: API key validation, JWT verification, token introspection,
RBAC policy enforcement, key rotation.

Part of the `serve/` category in Tattva OS.
