# serve/uembedding — Embedding Endpoint

> Vector embedding generation for RAG and semantic search.

Accepts text input, returns dense vector embeddings.
Supports: multiple embedding models, batch encoding, normalization.

Part of the `serve/` category in Tattva OS.
