# pkg/packages — Built-in Packages

> Core packages bundled with Tattva OS distributions.

These packages are pre-installed in every Tattva OS image.
Includes: runtime libraries, system tools, standard utilities.

Part of the `pkg/` category in Tattva OS.
