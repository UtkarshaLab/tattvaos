# pkg/format — UPK Package Format

> Binary package format specification and parser for UPM.

Defines: `.upk` file header, metadata schema, symbol export table,
binary payload layout, integrity checksum format.

Part of the `pkg/` category in Tattva OS.
